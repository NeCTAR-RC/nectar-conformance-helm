apiVersion: v2
name: nectar-conformance
description: 'Read-only conformance dashboard for Nectar. Deploy one release per tier (prod, test): a CronJob evaluates every site over PuppetDB and writes reports to a shared RWX volume, and a web Deployment serves them.'
type: application
version: 0.4.6
appVersion: 0.6.0
